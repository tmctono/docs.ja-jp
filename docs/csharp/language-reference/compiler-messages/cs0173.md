---
title: コンパイラ エラー CS0173
ms.date: 08/14/2018
f1_keywords:
- CS0173
helpviewer_keywords:
- CS0173
ms.assetid: eb1797ad-bf62-4e2b-8922-bef4aff36954
ms.openlocfilehash: 48ff73e5bfe70a8fcec16973ce1325880b436217
ms.sourcegitcommit: 559259da2738a7b33a46c0130e51d336091c2097
ms.translationtype: HT
ms.contentlocale: ja-JP
ms.lasthandoff: 10/22/2019
ms.locfileid: "72771826"
---
# <a name="compiler-error-cs0173"></a><span data-ttu-id="eddd2-102">コンパイラ エラー CS0173</span><span class="sxs-lookup"><span data-stu-id="eddd2-102">Compiler Error CS0173</span></span>

<span data-ttu-id="eddd2-103">'class1' と 'class2' の間に暗黙的変換がないため、条件式の型を決定できません。</span><span class="sxs-lookup"><span data-stu-id="eddd2-103">Type of conditional expression cannot be determined because there is no implicit conversion between 'class1' and 'class2'</span></span>

<span data-ttu-id="eddd2-104">クラス間の変換は、さまざまなクラスのオブジェクトと同じコードを使用する際に便利です。</span><span class="sxs-lookup"><span data-stu-id="eddd2-104">Conversions between classes are useful when you want objects of different classes to work with the same code.</span></span> <span data-ttu-id="eddd2-105">しかしながら、連動する 2 つのクラスに相互変換や冗長的変換を使用することはできません。または暗黙的変換を使用しないことはできません。</span><span class="sxs-lookup"><span data-stu-id="eddd2-105">However, two classes that work together cannot have mutual and redundant conversions, or no implicit conversions.</span></span> <span data-ttu-id="eddd2-106">`class1` と `class2` の型は別々に決定されます。より一般的な型は条件式の型として選択されます。</span><span class="sxs-lookup"><span data-stu-id="eddd2-106">The types of `class1` and `class2` are determined independently, and the more general type is selected as the type of the conditional expression.</span></span> <span data-ttu-id="eddd2-107">型の決定方法に関する詳細については、「[Conditional operator cannot cast implicitly](https://stackoverflow.com/questions/2215745/conditional-operator-cannot-cast-implicitly/2215959#2215959)」 (条件演算子は暗黙的に型変換できない) を参照してください。</span><span class="sxs-lookup"><span data-stu-id="eddd2-107">For more information about how types are determined, see [Conditional operator cannot cast implicitly](https://stackoverflow.com/questions/2215745/conditional-operator-cannot-cast-implicitly/2215959#2215959).</span></span>

<span data-ttu-id="eddd2-108">CS0173 を解決するには、変換の方向またはクラスに関係なく、`class1` と `class2` の暗黙的変換が 1 つだけ存在することを確認します。</span><span class="sxs-lookup"><span data-stu-id="eddd2-108">To resolve CS0173, verify that there is one and only one implicit conversion between `class1` and `class2`, regardless of which direction the conversion is in and regardless of which class the conversion is in.</span></span> <span data-ttu-id="eddd2-109">詳細については、「[ユーザー定義の変換演算子](../operators/user-defined-conversion-operators.md)」と「[組み込みの数値変換](../builtin-types/numeric-conversions.md)」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="eddd2-109">For more information, see [User-defined conversion operators](../operators/user-defined-conversion-operators.md) and [Built-in numeric conversions](../builtin-types/numeric-conversions.md).</span></span>

## <a name="example"></a><span data-ttu-id="eddd2-110">例</span><span class="sxs-lookup"><span data-stu-id="eddd2-110">Example</span></span>

<span data-ttu-id="eddd2-111">次の例ではコンパイラ エラー CS0173 が生成されます。</span><span class="sxs-lookup"><span data-stu-id="eddd2-111">The following examples generate compiler error CS0173:</span></span>

```csharp
public class C {}

public class A
{
    // The following code defines an implicit conversion operator from
    // type C to type A.
    //public static implicit operator A(C c)
    //{
    //    A a = new A();
    //    a = c;
    //    return a;
    //}
}

public class MyClass
{
    public static void F(bool b)
    {
        A a = new A();
        C c = new C();

        // The following line causes CS0173 because there is no implicit
        // conversion from a to c or from c to a.
        object o = b ? a : c;

        // To resolve the error, you can cast a and c.
        // object o = b ? (object)a : (object)c;

        // Alternatively, you can add a conversion operator from class C to
        // class A, or from class A to class C, but not both.
    }

   public static void Main()
   {
      F(true);
   }
}
```

```csharp
class M
{
    static int Main ()
    {
        int X = 1;
        // The following line causes CS0173.
        object o = (X == 0) ? null : null;
        return -1;
    }
}
```
