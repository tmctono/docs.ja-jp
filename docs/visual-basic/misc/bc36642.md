---
title: Option Strict On では、各ラムダ式のパラメーターの型を推論できない場合、そのパラメーターを 'As' 句で宣言する必要があります。
ms.date: 07/20/2015
f1_keywords:
- bc36642
- vbc36642
helpviewer_keywords:
- BC36642
ms.assetid: 2aaa62b8-49c9-4ae8-b0f5-08e3f0b5ad10
ms.openlocfilehash: 6d0d509ba2465b1a9300b759cc258781e2e183a9
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: ja-JP
ms.lasthandoff: 04/23/2019
ms.locfileid: "61970821"
---
# <a name="option-strict-on-requires-each-lambda-expression-parameter-to-be-declared-with-an-as-clause-if-its-type-cannot-be-inferred"></a><span data-ttu-id="ff15b-102">Option Strict On では、各ラムダ式のパラメーターの型を推論できない場合、そのパラメーターを 'As' 句で宣言する必要があります。</span><span class="sxs-lookup"><span data-stu-id="ff15b-102">Option Strict On requires each lambda expression parameter to be declared with an 'As' clause if its type cannot be inferred</span></span>
<span data-ttu-id="ff15b-103">ラムダ式で `As` 句を使用せずにパラメーター宣言し、 `Option Strict` がオンになっています。</span><span class="sxs-lookup"><span data-stu-id="ff15b-103">You have declared a parameter in a lambda expression without using an `As` clause, with `Option Strict` on.</span></span>  
  
```  
' Not valid when Option Strict is on.  
' Dim increment1 = Function (n) n + 1  
```  
  
 <span data-ttu-id="ff15b-104">`n` の型が推論できる場合、前の宣言は有効です。</span><span class="sxs-lookup"><span data-stu-id="ff15b-104">The previous declaration is valid if the type of `n` can be inferred.</span></span> <span data-ttu-id="ff15b-105">たとえば、前のラムダ式を関数デリゲート `Del`に割り当てられる場合があります。</span><span class="sxs-lookup"><span data-stu-id="ff15b-105">For example, if you are assigning the previous lambda expression to a function delegate, `Del`:</span></span>  
  
```  
Delegate Function Del(ByVal p As Integer) As Integer  
```  
  
 <span data-ttu-id="ff15b-106">`n` の型はパラメーター `p`から推測できます。</span><span class="sxs-lookup"><span data-stu-id="ff15b-106">Now the type of `n` can be inferred from parameter `p`:</span></span>  
  
```  
Dim increment2 as Del = Function(n) n + 1  
```  
  
 <span data-ttu-id="ff15b-107">**エラー ID:** BC36642</span><span class="sxs-lookup"><span data-stu-id="ff15b-107">**Error ID:** BC36642</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="ff15b-108">このエラーを解決するには</span><span class="sxs-lookup"><span data-stu-id="ff15b-108">To correct this error</span></span>  
  
- <span data-ttu-id="ff15b-109">`As` 句をパラメーター宣言に追加します。</span><span class="sxs-lookup"><span data-stu-id="ff15b-109">Add an `As` clause to the parameter declaration:</span></span>  
  
    ```  
    Dim increment3 = Function (n As Integer) n + 1  
    ```  
  
## <a name="see-also"></a><span data-ttu-id="ff15b-110">関連項目</span><span class="sxs-lookup"><span data-stu-id="ff15b-110">See also</span></span>

- [<span data-ttu-id="ff15b-111">ラムダ式</span><span class="sxs-lookup"><span data-stu-id="ff15b-111">Lambda Expressions</span></span>](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md)
